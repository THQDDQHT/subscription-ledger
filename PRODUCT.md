# Product

## 2026-09-12 全屏工作台方案

用户确认并已实施：固定导航、全宽清单、独立列表滚动、宽屏并排详情。近期、全部、备份分为独立视图，全部页隐藏统计；侧栏可折叠，平板自动紧凑，手机采用底部导航与原生模态详情。URL hash 保留视图和选中记录。此决策取代下文早期的居中或双栏布局描述。继续使用原生前端，无新增构建步骤或运行依赖；CSP 保持不变。最新验收见 docs/acceptance.md。

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

既有代码库已确定：Flask 3.1 + SQLite + 原生 HTML/CSS/JS，无 package.json、无 JS 依赖，运行时仅 3 个 Python 依赖。

**2026-09-10 用户确认松绑：允许引入前端构建步骤。** 这是对 `README.md`（"Node 仅用于前端回归测试"）与 `docs/requirements.md`（"轻量后端 + SQLite + 原生 HTML/CSS/JS"）既有约束的一次变更，三条后果尚未决定，未落地前保持旧约束有效：

1. **CSP 不得松绑。** `app.py:92` 当前为 `default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self'; connect-src 'self'`，无 `'unsafe-inline'`。打包产物必须外链 CSS/JS，不得内联 `<style>`/`<script>`，不得依赖 `'unsafe-inline'`。
2. **交付面从"拷贝 4 个文件"变成"需要构建产物"。** `Dockerfile` 目前只 `COPY static` / `COPY templates`，需增加构建阶段或将产物提交入库，否则镜像里是旧界面。
3. **零 JS 依赖的供应链属性会失去。** 该应用持有个人财务数据，当前 JS 依赖数为 0；引入打包器会带入数百个传递依赖。这是用攻击面换交付速度，属于需要知情同意的取舍。

已决定：不引入 CDN（`README.md` 的"不依赖外部资源"与 CSP 双重约束）。字体如自托管须走 `'self'`。

## Users

单一用户，即所有者本人。在两种场景下交替使用：

- **手机、随时**：想知道"最近要付什么"，随手瞥一眼，可能躺在床上关灯后。单手操作。
- **桌面、月度**：坐下来对账，批量整理状态、确认续费、导出备份。

不服务第二个人：无注册、无团队、无分享、无 OAuth。

## Product Purpose

私人订阅账本：记住订了什么、最近哪些要续费、周期支出大概是多少。所有金额固定人民币。手动录入，不接银行、不读邮箱、不自动扣款、不代为取消、无分析遥测。

成功的样子：用户打开它就知道最近要付什么，并且**相信这个数字**——包括相信它没有夸大。

## Positioning

**一个会声明自己局限的账本。** 每个头条数字下方立即写明它是什么："折算预算，不是实际账单"、"按周期展开，不代表已经扣款"。产品明确声明不自动扣款、不取消订阅、不主动提醒。

配套的是"只在你确认后记账"的机制："已续费"要求用户明确勾选"我已核对并完成本次续费"，且实际续费日期不能是未来（`actual_date.max = stats.today`）。它刻意拒绝冒充银行。

任何相邻产品都能做订阅列表；能诚实地拒绝制造虚假安心感的很少。

## Operating Context

- 日期一律按 Asia/Shanghai 解释，采用 `YYYY-MM-DD`，避免浏览器时区偏移。
- 统计口径是产品的一部分，且必须可见：季付按三个日历月递推（不是 90 天）并保留原始日/月锚点（1 月 31 日 → 4 月 30 日 → 7 月 31 日）；月度等效 = 月付原值 / 季付 ÷3 / 年付 ÷12 / 自定义天数 ×365÷天数÷12；合计后四舍五入到分。
- 提醒窗口为 `[今天, 今天+7/30天)`，逾期单列。已取消续费与已结束不计入预算。
- 状态词汇有真实区分：「准备取消」仍计入预算，「已取消续费」排除。
- 桌面为列表主区 + 近期续费侧区，手机单列。
- 数据可整体 JSON 导出/恢复；恢复前服务器先备份旧库到 `data/backups/*.sqlite3`，同一写锁内事务覆盖。

## Capabilities and Constraints

**已确认功能**：密码登录（scrypt 哈希，12–1024 字符）；订阅 CRUD（名称、每期金额、月/季/年/自定义天数周期、下次日期、自动续费标识、状态、服务截止日、管理链接、备注）；手工确认续费并保留锚点；周期预测；JSON 导出与严格校验的恢复。

**技术约束**：SQLite 持久化，跨设备共用；链接仅接受无凭证 http/https；请求体上限 2 MiB；登录限速（每 IP 15 分钟 5 次，反代不信任 X-Forwarded-For）；HttpOnly + SameSite=Strict 会话，12 小时有效；写请求校验 CSRF；CSP 见 Stack 节。

**规模（2026-09-10 确认）**：稳态 **100 条以上**订阅。当前 10 条种子数据不代表目标规模；搜索、分组、折叠/虚拟滚动属于必需能力，不是可选优化。

**验收纪律（既有，未变更）**：`pytest -q`、`scripts/http_smoke.py`、`node scripts/session_regression.cjs` 三条须保持全绿；改动记录进 `docs/acceptance.md`。改了 UI 不构成豁免理由。

**明确未决定**：构建步骤的具体形态与产物落盘方式（见 Stack）；暗色模式是否随重构一并提供。

## Brand Commitments

- 名称「订阅账本」。
- `static/favicon.svg`：自绘 64×64 SVG，`#006b61` 圆角日历页 + 环形续费箭头，不用 CDN。目前是项目最有识别度的资产，且未出现在页面内。
- 主色 `#006b61`；数字使用 `font-variant-numeric: tabular-nums` 且金额右对齐（实测 `¥12.00` 与 `¥888.00` 按小数点对齐）。这是产品的性格，重构不得为了好看而牺牲。
- 语气：直白、不推销、不制造焦虑，宁可暴露局限也不含糊。

## Evidence on Hand

- `README.md`、`docs/requirements.md`、`docs/acceptance.md`、`docs/plans/`：产品边界、使用口径、验收与安装记录。
- `docs/plans/mvp.md`、`docs/plans/quarterly-layout.md`：既有迭代记录。
- `tests/test_ledger.py`、`test_quarterly.py`、`test_extra.py`、`test_regressions.py`：领域与回归覆盖。
- `scripts/http_smoke.py`、`scripts/session_regression.cjs`、`scripts/copy_to_host.py`：端到端与前端会话回归。
- `.impeccable/critique/2026-09-10T08-27-29Z__templates-index-html.md`：本轮评审（18/40）与实测证据。
- `.impeccable/review/*.png`：1440×900 / 390×844 实测截图。

**不存在、后续工作不得虚构**：真实订阅数量与名称（种子数据是合成的）、用户量、性能基准、任何第三方客户或背书。`README.md` 已声明"源码及隔离验收，不是公网部署"、"真实手机浏览器验收仍需完成"——不得声称已部署或已通过真机验收。

## Product Principles

1. **数字必须可解释。** 任何聚合数字旁边就要有它的口径与局限。宁可显得啰嗦，也不让用户误以为某笔钱已经扣了。
2. **写入必须有回执。** 账本里无法确认的写入与丢失的写入无法区分。每次改动都要有可见、可区分于错误、且不打断位置的结果。
3. **账本的重心是"接下来要做什么"。** 逾期与近期续费优先于完整清单；完整清单是次要视图。
4. **状态是决策信息，不是元数据。** 使用中 / 准备取消 / 已取消续费 / 已结束 必须在一眼之内可辨，不能只靠读一句灰字。
5. **个人财务工具的最小攻击面是一项功能。** 每引入一个依赖或放宽一条 CSP，都要当作产品决策而不是实现细节。

## Accessibility & Inclusion

无正式合规标准要求。既有事实：文本对比度实测 0/30 失败（最低 5.53:1）；点击目标多数 ≥44px；全部表单控件有包裹 `<label>`；使用原生 `<dialog showModal()>`（焦点进入、背景 inert、Esc 可关）。

已实测的缺口，重构时一并解决：焦点环 `#de9919` 对比度 2.42:1（低于 WCAG 1.4.11 的 3:1）；两个弹窗无可访问名称；提醒窗口标签按钮不暴露选中态；桌面 DOM 顺序与视觉顺序相反。


## 2026-09-12 已实施的界面更新

用户批准调研方案后，将双栏改为「近期处理 / 全部订阅」统一工作区；近期优先，新增搜索、状态筛选、组内排序和可折叠分组，低频信息与操作放入详情。延续原生前端，无构建依赖。每个当前计划只保留一个续费操作入口；未来事件展开仍仅用于预测统计。保留随系统变化的深色主题。此前 Accessibility 小节的旧缺口已在当前源码补上：青绿色焦点环、弹窗 aria-labelledby、视图 aria-pressed 与一致的 DOM 阅读顺序。本轮结果见 docs/acceptance.md。

## 2026-09-12 视觉方向（第二轮）

用户从「精修现有风格 / 更有辨识度 / 整体重做」中选定「更有辨识度」：布局与青绿主色不变，工作区改为纸面色底 + 白色账页 + 分栏表格，金额成为每行视觉锚点。用户否决了深青色侧栏（「颜色太丑」），最终侧栏为浅暖灰、青绿只用于品牌标、当前项与主按钮；侧栏底部「单人使用 · 人民币 / 日期按中国标准时间」说明按用户要求删除。数据备份曾短暂移到右上角菜单，用户随后要求放回侧栏：现在侧栏为「近期处理 / 全部订阅 / 数据备份」，左下角是外观切换与退出登录；手机底栏保留三个视图，外观切换与退出放在备份页底部。用户问及换肤后新增「跟随系统 / 浅色 / 深色」三段切换，偏好存浏览器本地，不做多套配色皮肤。交互一并落地：整行可点、近期页行内续费、中文日期与剩余天数、页面内确认弹窗、快捷键。仍无 DESIGN.md；如需固化令牌与组件规范，可运行 `/impeccable document`。
