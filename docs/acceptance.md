# 核心验收记录

## 2026-09-15 独立进程镜像依赖修复

- 首次镜像部署在执行备份时发现 `maintenance.cjs` 无法解析 `better-sqlite3`，数据库未迁移；原网页容器已恢复运行。
- Next standalone 已含 SQLite 实际文件，但缺少独立 CLI 所需的根模块入口。构建 worker 后为已追踪包补充相对链接，保持镜像可搬迁。
- HTTP 烟测将全部 standalone 产物复制到源码目录之外，保留相对链接并清除 `NODE_PATH` 后启动网页和 worker。旧产物真实复现缺依赖错误，修复后 12 组检查全部通过。
- Docker 最终运行层在镜像构建时执行临时库迁移、备份、单轮 worker 和健康检查；线上切换前还将用 Compose 临时目录复验，避免依赖问题出现在停服之后。

## 2026-09-15 镜像 Skill 打包校验

- Docker 构建上下文为 Skill 的 `SKILL.md` 增加例外，避免通用 `*.md` 排除规则让线上 ZIP 缺少说明文件。
- 打包前检查 `SKILL.md` 与 Python 客户端必须存在；本地验证正常 ZIP 含两项文件，缺少说明文件时立即失败且不写入下载包。最终镜像发布后还需检查线上 ZIP 内容。

## 2026-09-15 设置页视觉整理

- 移除设置页固定最大宽度：导出与恢复两个小卡片并排，Telegram 与 Agent 大卡片各占整行。1920px 视口下两侧内容边距均为 24px，大卡片宽 1648px；窄屏自动堆叠。
- 大卡片内部横向分区：Telegram 连接信息与提醒偏好并排，宽屏四个字段同排，开关移至标题右侧，后台状态收在底部运行记录；Agent 创建令牌与文档、Skill、已有令牌分居左右两栏。
- 表单间距统一，保存反馈就近显示；保存后收起私聊选择，隐藏完整令牌后移除过期提示。备份说明精简，页面脚注移至底部。
- 生产构建通过。隔离 Chrome 验证通知校验/保存、私聊选择、键盘开关、测试确认取消、令牌创建/隐藏/撤销、文档和下载入口；1920、1440、1132、768、390、375 像素及 844×390 横屏无溢出，深浅色和减少动画模式通过，手机操作按钮至少 44px。
- 未发送真实 Telegram 消息。最终合成截图及验收脚本保存在本机 artifacts/settings-fullwidth/，本次未部署。


## 2026-09-15 余额账户、独立 Telegram 与 Agent 接口

- 已实现固定/估算余额账户、充值、按今天实际余额校正、按实际账单总额调整差额；流水保留来源与关联账期，补录早于余额核对的历史账单不再改变当前余额。
- 独立 worker 每分钟执行，关闭通知也会记账；使用同一 Compose 镜像和现有 SQLite 数据卷。独立 Bot 的配置、最近私聊选择、发送状态和测试入口位于备份与设置。Agent 令牌分只读/读写，支持创建、仅一次显示与撤销。
- 提供 OpenAPI、可下载 Hermes Skill 和 Python 客户端。根据 Hermes 官方格式声明 required_environment_variables；验证了共同 Skill 元信息及 ZIP 内容，未修改 Hermes 本体。
- Vitest：7 个文件、250 次测试执行全部通过（原有测试文件导入 domain.test.ts 会重复执行领域测试，数量不是独立业务场景数）。新增覆盖停机补扣、月底与闰年、事务回滚、估算转实际、跨账户引用、充值/校正、Agent 权限与去重、v1/v2 恢复、提醒阈值、发送认领和失败重试、私聊发现脱敏、前端分组。
- 生产构建通过；12 组 HTTP 烟测通过。使用真实 Next standalone、两个并发 worker 和 Python Skill 客户端验证周期只扣一次、重试充值不重复、备份迁移、worker 健康检查和令牌撤销。全部运行在临时数据目录与随机回环端口，结束后清理。
- Playwright 驱动本机 Chrome：1440 桌面与 390 手机下验证新增余额账户、默认日期、充值和校正后刷新、手机实际账单仅调整差额、自定义两个月、原生模态及深色设置；无页面 JS 错误和手机横向溢出。合成数据截图在本机 artifacts/prepaid/（忽略提交），不是实体手机/软键盘验收。
- 服务器只读预检确认仍运行 e8bbc57305db41d8813472cebeed0640e28aab28，工作区干净、数据卷与端口匹配部署计划。本轮未 push、迁移生产库、重启或配置真实 Bot。真实 Telegram 和 Hermes 运行时接入待部署后验证。
- 原有 Vite 的 __dirname 配置警告仍存在，不影响测试；本机未构建 Docker 镜像，镜像构建与服务器运行验证属于部署步骤。


## 2026-09-15 登录有效期延长至 30 天

- 登录状态由 12 小时改为固定 30 天，Cookie 的 `Max-Age` 和服务端签名凭证的 `exp` 使用同一有效期；访问不续期，主动退出立即清除浏览器登录状态。升级前已有凭证保留原期限，重新登录后使用新期限。
- 新增 API 回归：登录后 13 小时、第 29 天、满 30 天前 1 毫秒仍可访问；满 30 天立即返回 401；访问不重新下发 Cookie；主动退出清除 Cookie；旧凭证在原 12 小时边界失效。
- `pnpm test`：6 个文件、236 次测试执行通过；`pnpm build`、`pnpm smoke` 9 组通过。使用隔离临时数据库验证，未部署。

## 2026-09-15 半年付与自定义月数、年数

- 计费周期新增半年付、自定义月数和自定义年数；自定义数量使用独立字段并校验正整数范围。列表、详情、编辑预览和续费弹窗显示对应数量及单位。
- 日期递推、月度预算、预测快进支持新周期。按日历保留月底/闰年锚点，修改周期数量会按当前计划日期重置锚点；备份往返保留数量与历史，原天数周期及旧备份兼容。
- `pnpm test`：6 个测试文件、233 次测试执行通过（现有 API 测试文件会导入并重复执行领域测试）；`pnpm build` 和 `pnpm smoke` 9 组通过。Vite 关于现有 `__dirname` 配置的提示仍在，与本次改动无关。
- 使用隔离临时数据库、随机密码和 loopback 端口，对生产构建执行 Chrome 浏览器验证：半年付创建、每 2 个月创建及续费、数量校验、编辑回填、数量变更后锚点重置、刷新持久化、切换月数/年数/天数、每 2 年闰年续期。1440×1000、375×812、812×375 无横向溢出，浏览器无控制台错误。截图与结果保存在本地 `artifacts/custom-cycles/`。
- 验证后已关闭临时浏览器和服务、删除临时数据库。本次为本地实现，尚未部署；手机验证为浏览器视口模拟。

## 2026-09-14 全栈 TypeScript 重写（Next.js 16 单体）

- 用户决定废弃 Python，全栈统一 TS。调研否决「Vite SPA + Flask」（用户不希望保留 Python）与「Next 仅作前端」；选定 Next.js 16 App Router 单体：页面全部为客户端组件（登录墙后 RSC/SSR 无收益），API 走 Route Handlers（单一 catch-all `src/app/api/[...path]/route.ts` 委托框架无关的 `src/server/api.ts`，鉴权/CSRF/限流集中一处，对应原 before_request）。
- 移植策略：**原 pytest 套件即行为规格，先移植测试再移植实现**。`domain.py` → `src/server/domain.ts`（summary 经可变 `domain` 命名空间调用 advance，保留原猴子补丁测试的拦截语义）；月度预算用 BigInt 有理数精确求和后 HALF_UP 到分，不引入浮点误差。`app.py` 12 个端点逐行平移；better-sqlite3 的 `backup()` 不能跑在持有写事务的同一连接上，恢复前备份改走独立只读连接（语义等价）。
- 兼容：werkzeug scrypt/pbkdf2 哈希格式用 `crypto.scryptSync`/`pbkdf2Sync` 双向互验通过（Node 生成 → Python 校验、Python 生成 → Node 校验均 True），**现有 data/ 目录沿用，不用重置密码**；旧库自动补列迁移幂等性有测试。会话 Cookie 改为 HMAC 签名 JSON（密钥复用 data/session.key），旧会话一次性失效需重新登录。登录限速改为全局单桶（Node 路由层无对端 IP，语义同原反代部署）。
- 前端 1:1 平移：原 style.css 组件层整体迁入 `globals.css` 的 `@layer components`（类名不变），令牌注册为 Tailwind `@theme inline` 双主题变量；React 复刻原 DOM 结构与全部文案；原生 `<dialog>` 保留（宽屏 `show()` 并排 / 窄屏 `showModal()`）；图标换 lucide。CSP 用每请求 nonce（`src/proxy.ts`），script-src 保持严格，style-src 放宽 `'unsafe-inline'`（Radix 内联样式，已在方案中获批）；静态哈希资源 immutable 缓存，其余 no-store。
- 验证：vitest **136 项**全绿（domain 28 + API 97 + 前端 11）；`pnpm smoke`（HTTP 烟测移植版）**9 组**通过；`pnpm build` 通过（TS 7）。playwright-core 驱动本机 Chrome 对生产构建（.next/standalone）做 **48 项**交互检查全过：登录/快捷键（/ N Esc）/搜索/筛选/排序/分组折叠/侧栏折叠/整行点击/宽屏并排详情/手机全屏模态/编辑器天数联动与计划预览/续费默认实付与建议日期/撤销确认取消与接受/焦点恢复/深色切换与刷新保持/导出下载/坏备份就地拒绝/触控目标 ≥44px/1440·1280·900·390 无横向溢出/控制台无 error/warn。
- 真实数据目录副本（含 werkzeug 哈希与旧库）冷启动验证：旧库打开迁移无误、configured:true、错误密码 401、未登录 401。截图已替换 docs/ui/*.png（合成数据）。
- 未做：Docker 镜像未构建（配置已更新为 node:24-alpine 多阶段）；真实手机验收；pnpm 11 注意 `allowBuilds` 取代 `onlyBuiltDependencies`（白名单仅 better-sqlite3、esbuild）。开发期 Next dev 需用 localhost 或 127.0.0.1 加 `allowedDevOrigins`（已配置）。

### 2026-09-14 后续修正与 CI

- 修复 dev 模式 CSP 报错：React 开发模式需要 `eval()`，`src/proxy.ts` 的 `script-src` 现仅在 `NODE_ENV=development` 时附加 `'unsafe-eval'`，生产保持严格（nonce + strict-dynamic）。用户 dev 服务器（localhost:8765）实测：登录页渲染、控制台零错误。
- 新增 `.github/workflows/ci.yml`：push 到 main 或 `v*` 标签时依次跑 `pnpm test` → `pnpm build` → `pnpm smoke`，通过后 buildx 构建 linux/amd64+linux/arm64 镜像并推送 `ghcr.io/thqddqht/subscription-ledger`（分支打 `main`、标签打 semver、另有 sha 标签）；PR 只跑测试不推镜像。镜像本身尚未实际构建验证，首次以 CI 结果为准。

## 2026-09-12 续费历史与实付金额

- 用户从功能建议中选定「续费历史展示与撤销」「续费时记录实付金额」。后端：`renewals` 表新增可空列 `amount_cents`、`recorded_at`，旧库首次打开自动 `ALTER TABLE` 补列（幂等）；续费接口接受可选 `amount`（与订阅金额同一校验规则，缺省取当前每期金额），写入实付金额与 Asia/Shanghai 记录时间，返回 `renewal_id`；新增 `GET /api/items/<id>/renewals`（最新在前，服务端标记 `undoable`）与 `POST /api/items/<id>/renewals/<rid>/undo`（需 `confirm:true`；只允许最近一次且 `next_date` 仍等于当前计划日期，否则 400 并说明原因；退回 `previous_date`、删除该条历史，不改金额、状态与锚点；不限制状态）。导出改为按写入顺序输出历史并附 `amount`/`amount_cents`/`recorded_at`；恢复以 `amount_cents` 为准，缺失或 null 视为未记录，非法分值、布尔、超限或不可解析时间戳一律拒绝且不改库。`version=1` 不变。
- 前端：续费弹窗新增「实际支付金额」字段，默认填入每期金额，回执含实付金额；详情备注下方新增「续费历史」区块（打开详情时异步加载，带 `aria-label`、加载中/失败重试/空态文案），条目显示实际续费日、原计划 → 下次、实付金额（无金额的早期记录标「未记录金额」）、记录时间到分钟；标题右侧给出「共 N 次 · 实付合计」，早期无金额记录如实说明并不计入。最近一次可撤销的记录带「撤销这次续费」按钮，页面内确认写明退回日期、被删记录与不受影响的内容；不可撤销时说明「此后计划日期已被编辑」。历史缓存随 `load()` 与会话失效一起清空。样式沿用账页令牌，触屏保持 44px 触控高度，鼠标环境收紧为 32px。
- 验证：42 项 pytest（新增 5 项：金额与时间写入、撤销顺序与锚点保留、编辑日期后拒绝撤销、备份往返与旧备份兼容、旧库迁移）、9 组 HTTP 烟测（新增实付金额/历史/撤销一组）、会话回归（新增历史缓存清理断言）与 UI 回归（新增历史摘要与记录时间格式）全部通过；`node --check`、`git diff --check` 通过。用 playwright-core 驱动本机 Chrome（无头，安装在仓库外临时目录，未加入项目依赖）对隔离预览服务做 24 项交互检查：空态、默认金额、修改实付金额后的回执与条目、合计、订阅金额未被改写、撤销确认取消/接受、撤销后日期退回与焦点位置、编辑日期后不可撤销说明、390px 无横向溢出、手机弹窗字段堆叠、触屏按钮 44px、控制台无 error/warn。截图存 docs/ui/detail-history-desktop.png、detail-history-desktop-dark.png、detail-history-mobile.png（合成数据）。
- 未做：真实手机与软键盘验收；未改 Docker、Nginx 或任何正式数据库；预览服务与临时数据在验收后已停止并删除。撤销依赖「计划日期未被改动」这一不变量，续费后又手工改过日期的记录只能通过编辑日期纠正，属有意为之。

## 2026-09-12 备份页重做

- 用户反馈备份页「不太好看」。改为「导出备份 / 恢复备份」两张卡片（桌面并排、手机堆叠）：导出卡片列出内容条数、文件名与本浏览器上次导出时间（localStorage 仅存时间戳）；恢复卡片标题带「覆盖操作」标签，改为拖放区（支持拖入与点击，原生 file input 视觉隐藏但可聚焦），选中后本地解析并显示订阅数、续费历史数、文件大小，格式错误就地标红且恢复按钮保持禁用，可一键移除所选文件；确认弹窗写明文件名与替换条数。产品声明文案移到页面底部作脚注。服务端校验逻辑未变。机械检测器曾提示卡片「图标块叠标题」为模板化写法，已去掉图标块。拖放区悬停高亮限定鼠标设备，触屏点击后不会卡在高亮态。
- 验证：37 项 pytest、8 组 HTTP 烟测、两个 Node 回归通过；Playwright 49 项只读交互检查通过，新增：导出事实行、错误文件被拒且按钮禁用、有效文件摘要与按钮激活、确认文案含文件名、移除文件后复位。控制台无 error/warn。1440 浅色/深色与 390 手机截图核对通过，截图存 docs/ui/backup-desktop.png、backup-mobile.png。

## 2026-09-12 精修（第三轮）

- 用户从四项候选中选定三组（未选「详情显示续费历史」，数字字体保持系统字体）：降噪（鼠标环境下行内「记录续费」悬停/聚焦/选中时才出现，触屏常显；删除「点击记录查看详情 · 人民币」常驻提示）；排版（去列间竖线，表头与分组标题吸顶并带 `backdrop-filter` 毛玻璃，货币符号缩小变淡、数字放大到 16px，统计区与详情同样处理）；动态（首次加载超过 120ms 显示骨架行并标记 `aria-busy`，加载失败给出重试；手动切换深浅色 250ms 颜色过渡；分组展开时行淡入）。
- 验证：37 项 pytest、8 组 HTTP 烟测、两个 Node 回归通过。Playwright 44 项只读交互检查通过，新增：悬停前后按钮 opacity 0→1、键盘聚焦显现、货币符号独立元素、滚动后表头 0px / 分组标题 36px 吸顶、API 延迟 700ms 时骨架出现并被真实行替换、无常驻提示文案。控制台无 error/warn。
- `color-mix()` 与 `backdrop-filter` 需 Chrome 111 / Safari 16.2 以上；不支持时表头退化为不透明背景，功能不受影响。docs/ui 截图已更新。仍未做真实手机验收；未改后端。

## 2026-09-12 界面性格与交互（第二轮）

- 用户选定「更有辨识度」方向：保留布局与青绿主色，改为纸面色工作区、白色账页、分栏表格；深青侧栏方案被用户否决后改为浅暖灰侧栏。侧栏底部说明文字删除。备份入口曾短暂移到右上角菜单，用户要求放回侧栏：现为「近期处理 / 全部订阅 / 数据备份」三项，左下角为外观切换与退出登录；手机底栏三个视图，备份页底部提供外观切换与退出。
- 外观切换：跟随系统 / 浅色 / 深色，偏好存浏览器 localStorage（仅主题名，不含账本数据）；`static/theme.js` 在首帧前写入 `data-theme`，CSS 深色令牌同时提供「跟随系统」与「手动指定」两条路径。
- 交互：整行可点开详情（按钮与选中文字除外）；近期页逾期与 7 天内的行内「记录续费」；分组标题显示条数与每期金额小计；金额千分位；日期显示相对天数 + 中文日期（周几），`<time>` 保留 ISO；快捷键 `/`、`N`、`Esc`；删除与恢复备份改为页面内 `<dialog>` 确认并写明影响记录数；空态带下一步按钮。
- 验证：37 项 pytest、8 组 HTTP 烟测、会话回归与 UI 回归（新增金额/日期/相对天数断言）全部通过。Playwright 驱动本机 Chrome 对 1440×900 与 390×844 做 36 项只读交互检查（快捷键、整行点击、行内续费入口、确认弹窗取消、侧栏顺序、主题切换即时生效与刷新后保持、手机模态详情、触控目标 ≥44px、卡片模式日期行对齐），全部通过，控制台无 error/warn。1440 / 1280 / 900 / 390 宽度列表与文档无横向溢出。折叠侧栏时分组条数曾被误隐藏，已修正。
- Impeccable 机械检测器：无 tiny-text；剩余 3 条 cramped-padding 为账页容器与 `<dialog>` 的误报（子元素各自带内边距，容器本身不直接承载文字）。浅色弹窗用阴影、深色用描边，不再同时叠加。
- docs/ui 截图已替换为本轮界面。仍未做真实手机、软键盘验收；未改后端、正式数据库、Docker 或 Nginx；预览服务与样例库未动。

## 2026-09-12 全屏工作台

- 已落地固定侧栏、全宽清单、独立列表滚动与固定表头。近期页保留统计，全部页直接显示清单，备份独立成页。
- 宽屏详情使用原生 dialog.show() 并排展示；窄屏使用 showModal()，保留焦点隔离和 Escape。手机使用底部导航，备份页可退出登录。
- URL hash 保留视图与选中记录，支持刷新和返回；会话失效清除详情、计数、选择和 URL 中的记录 ID。
- 验证通过：37 项 pytest、8 组 HTTP 烟测、两项 Node 回归、语法与 diff 检查。新增导航解析、详情链接、近期状态筛选回归。
- 使用 120 条样例记录完成浏览器搜索、详情联动、金额编辑、续费、手机返回、备份入口验收，控制台无 error/warn。390、768、1280px 宽度无列表横向溢出；1440x900 全部页首条记录约 y=223px，页面不再整体滚动。
- docs/ui 截图已更新。本地 8765 运行新版样例账本，保留样例库和会话；未改后端统计或部署生产服务。真实手机软键盘与深色模式仍需单独视觉验收。

本次接手保留原项目，没有从零重写。

## 实际执行结果

- workspace Python 3.13：pytest **24 passed in 2.42s**。
- 宿主机 `/home/q2qs/projects/subscription-ledger`，独立 Python 3.12 venv：pytest **24 passed in 2.11s**。
- 两端真实 TCP HTTP 烟测：**7 groups passed**，包括进程重启后库与会话持久化、恢复前 SQLite 备份、退出后拒绝读取；临时服务 loopback、执行结束终止并检查端口关闭。
- 两端 Node 前端回归通过：401 更新 CSRF；刷新请求离线时仍清除私人 DOM、状态和弹窗。
- workspace 压测：2000 条从 1900 年开始的每天续费记录，展开 60000 个未来事件，实际 **0.155 秒**。
- 复制时 21 个源文件逐文件 SHA-256 校验一致，不含本地 venv、数据、密钥和缓存。本文件为验收后补充。

## 修复

- 添加 pytest.ini，修复直接运行 pytest 时 app/domain 导入失败。
- 预测用算术快进跳过历史周期，保留月末/闰年锚点；新增回归先观察到 3 项失败，再修复通过。
- 会话失效清除私人界面并刷新 CSRF，登录前再次取 token；新增 Node 回归先失败、修复后通过。

## 安装问题及处理

宿主机 Python 缺少 ensurepip，默认 `python3 -m venv` 失败。未安装系统包、未 sudo，改用 `python3 -m venv --without-pip .venv`，从官方 HTTPS `https://bootstrap.pypa.io/pip/pip.pyz` 下载到 `.venv/pip.pyz`，通过以下命令把依赖仅安装到项目 venv：

```sh
python3 .venv/pip.pyz --python .venv/bin/python install -r requirements-dev.txt
```

## 尚未验收/上线

- 尚未进行真实手机浏览器主要操作验收（响应式样式存在，HTTP/Node 测试不能替代浏览器验收）。
- Docker 配置提供但依要求未构建/启动。
- 没有初始化用户正式密码、常驻应用服务、修改 Nginx、域名、生产服务或开放公网端口；这些需另行授权。


## 2026-09-12：订阅工作区 UI 改版

- 沿用 Flask + SQLite + 原生前端，无新增运行依赖或构建步骤。保留已有未提交修改中的状态标识、原生弹窗与安全处理，再重做布局。
- 新增近期/全部视图、名称与备注搜索、组内排序、可折叠分组、桌面详情侧面板、手机全屏详情、表单补充信息与保存回执。
- 近期列表按订阅当前计划分组，边界为逾期、[今天,今天+7天)、[今天+7天,今天+30天)；不再对预测事件逐次提供续费按钮，后端预测金额口径保持不变。
- pytest：37 passed；真实 HTTP 烟测：8 groups passed；会话回归与新增 UI 选择逻辑回归均通过。新回归覆盖日期边界、预测重复、名称/备注搜索、筛选排序、120 条记录及空结果。会话回归新增详情 DOM、详情弹窗、搜索和计数清理。
- 本地隔离库种入 120 条合成记录，浏览器完成名称搜索、详情长备注、编辑保存、手机新增自定义天数订阅、续费确认；验证续费后日期推进且搜索保留、焦点返回操作按钮。测试新增后为 121 条。浏览器未发现 error/warn 日志。
- 检查 1440×900 桌面与 390×844 手机视口，页面宽度未溢出；手机表单保存按钮固定可见。截图在 docs/ui/desktop.png、docs/ui/mobile.png，均为合成数据，不代表真实个人支出。
- 浏览器视口检查不等同于真实手机/软键盘验收；未部署公网，未改后端、正式数据库、Docker 或 Nginx。
